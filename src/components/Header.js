import React from "react"
import { Header, HeaderText } from "../elements"

export const Header = () => {
  return (
    <Header>
      <HeaderText>
        Welcome to my project page. This is where I keep them
      </HeaderText>
    </Header>
  )
}
